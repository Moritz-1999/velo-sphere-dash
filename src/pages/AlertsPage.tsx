import { PageLayout } from "@/components/layout/PageLayout";

const AlertsPage = () => (
  <PageLayout>
    <div className="p-4 flex items-center justify-center h-full">
      <div className="text-center space-y-2">
        <h1 className="text-lg font-semibold text-foreground">Alerts & Market Breadth</h1>
        <p className="text-xs text-muted-foreground">Alert Engine, Breadth Scanner & Custom Alerts — coming next</p>
      </div>
    </div>
  </PageLayout>
);

export default AlertsPage;
